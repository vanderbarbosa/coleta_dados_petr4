---
name: banca-julho2026-refino
description: "Ponderações da banca no seminário de jul/2026 e o refino (datasets, RSL, encoders, volatilidade) — vai para a dissertação"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9477fc8b-d091-44dd-9de3-ae811eed9316
---

Após o seminário de jul/2026 (boa avaliação), a banca fez ponderações; respostas documentadas em `docs/RESPOSTAS_BANCA_JUL2026.md` (tudo com fonte, nada inventado). Criada a pasta **`datasets_refino/`** (raiz do projeto) com geradores versionados (`_vN`, não sobrescreve — comparação):

- **`gerar_datasets_refino.py`** → `01_noticias_apos_17h` (54.259 notícias = **26,4%** do corpus, filtro `Data_Coleta`.hora≥17 = Brasília), `02_..._enriquecido` (por notícia: pregão via `merge_asof` p/ próximo pregão real, sentimento FinBERT, **Volatilidade_GARCH** do dia, retorno, direção real, parâmetros dos encoders), `03_resultados_volatilidade` (Granger+quantílica+regime). Fontes: `noticias_com_sentimento.csv` (215k linhas; colunas incl. Indice_Sentimento/Label/Score, Data_Coleta[Brasília], data_gmt[UTC], Data_Ajustada), `base_master_petr4.csv` (Date, **Volatilidade_GARCH**, Alvo…).
- **`gerar_rsl_dataset.py`** → `04_revisao_sistematica_estudos` (**25 estudos** da longtable `tab:rsl_artigos` do Cap.2 + títulos do `references.bib`). Método/encoder preenchidos com fonte; **Parâmetros e Resultados por estudo NÃO constam na síntese** → marcados "ver PDF" (PDFs em `Referencial_Teorico/`), não inventados.

**Itens 2 e 3 (resultados):** a banca notou (a) falta de volatilidade nos resultados e (b) direção "pior que moeda". Real (`resultados_modelos_petr4.csv`): XGBoost só-preços **49,77%** (≈acaso, não significativamente <50%), XGBoost DataFusion **52,22%** (+2,45pp do sentimento, mas p=0,145, não supera baseline ~53%). **Mensagem honesta:** direção é quase passeio aleatório (mercado eficiente); a contribuição forte/significativa é **volatilidade** — Granger sent→vol **p≤0,0002** em todas as defasagens; quantílica τ=0,05 **+261bps (p=0,034)** (viés de negatividade). Promover volatilidade ao corpo dos Resultados.

**Item 1 (Bloomberg Línea):** usa **Arc Publishing**, NÃO WordPress (`/wp-json` → 404; `robots.txt` mostra `/arc/outboundfeeds/` e bloqueia `/pf/api/v3/`). Google-News sitemap tem título+data mas só ~48h → inviável p/ corpus histórico; registrar como trabalho futuro (coletor forward via sitemap Arc, respeitando robots + termos/paywall).

**RSL (item 5) — completada com PDFs:** `04_revisao_sistematica_estudos` tem **14 colunas**, incl. **Fonte_Noticias** e **Metodo_Coleta** (extraídos lendo os resumos dos PDFs via PyMuPDF/`fitz`). Ex.: Tetlock=WSJ "Abreast of the Market"; Schumaker=Yahoo Finance; Bollen=Twitter(OpinionFinder+GPOMS); Groß-Klußmann=Reuters NewsScope; Calomiris=Thomson Reuters 1996-2015; Silva2018 e Cardoso2024=Valor Econômico; Narde2024=X/Twitter PT-BR (tarefa=fake news, não ações). Resultados preenchidos quando no resumo (Bollen~87,6%, Narde 95,1%, Nguyen +6,07%, Barak 83,6/88,2%); parâmetros detalhados = "ver artigo".

**Item 8 — experimento pronto:** `src/sentimento/12_finetune_albertina_ptbr.py` — fine-tuna encoder (Albertina PT-BR/DeBERTa; opções 900M, BERTimbau-large, turing-usp/FinBertPTBR) no **conjunto-ouro** (rótulos humanos + títulos do corpus por hash_titulo) e compara ao FinBERT-PT-BR no mesmo teste (acurácia, F1-macro, Kappa). Requer GPU + `pip install datasets` (HF datasets NÃO está no env petr4). Conjunto-ouro: `Mestrado_PETR4/conjunto_ouro/` (gabarito_modelo.csv + para_rotular.xlsx aba "Rotular").

**Rodada de experimentos (04/07/2026, execução autônoma):** `src/modelagem/13_experimentos_datafusion.py` — 15 configs (LogReg/SVM_RBF/RandomForest/XGBoost/Stacking × atributos base3/categoria/full) sobre `base_master_enriquecida_petr4.csv` (2610×38), split 60/15/25, limiar calibrado na validação, replicando técnicas da RSL (`05_tecnicas_alto_desempenho_rsl_v1.csv`: Barak=stacking, Nobre=tuning, Nguyen=ISM/categoria, Ballings=RF/AUC, Oliveira=walk-forward). **Melhor: XGBoost base3 = 54,52%** (AUC 0,514, MCC 0,068), **supera baseline 53,14%**, signif. vs acaso (binomial **p=0,012**) — CONFIRMA o 54,5% que a dissertação já reportava. Stacking/full NÃO superaram (sobreajuste). Cada teste virou dataset `datasets_refino/exp_2026-07-04_<modelo>_<atributos>.csv` (15) + consolidado `resultados_experimentos_datafusion_2026-07-04.csv/.json`. **Dissertação atualizada:** `capitulos/4d-experimentos-suite.tex` (tabela técnicas replicadas + tabela 15 experimentos + discussão), incluída via `\input` no Cap.4 (antes da Síntese). Nota: a diss (4-resultados.tex) já tinha tabela comparativa com literatura reportando 54,5%/ganho 4,4pp.

**Filtro de RELEVÂNCIA (05/07/2026) — testado, achado NEGATIVO honesto:** o orientando notou que faltava filtrar relevância antes de agregar o sentimento. Confirmado: `_agrega_ism.py` faz ISM diário = média de TODAS as notícias (sem filtro; filtro estrito `TERMOS_RELEVANCIA_ESTRITA` foi testado e descartado antes). `datasets_refino/gerar_pipeline_relevancia.py` reordenou o pipeline (coleta→após17h→relevância→predição) e re-treinou XGBoost (2018-2025, 60/15/25) em 3 variantes: todas_apos17h **52,11%**/AUC0,521, relevante_direta (menção direta, 23,3% das notícias) **52,11%**/AUC0,525, CAT1+CAT2 **50,91%**. **Filtro de relevância NÃO melhorou a acurácia direcional** (cobertura ~100% — todo dia tem notícia relevante; corpus já filtrado pela taxonomia; gargalo = eficiência do mercado). Granger sent→vol segue signif. (p<0,02) em todas. Datasets: `10_noticias_apos_17h`, `11_noticias_relevantes`, `12_relevantes_{Treino,Validacao,Teste}` (esquema exato pedido: índice único N%07d, sentimento, risco, **previsão da direção**, encoder/params, data). Dissertação: `capitulos/4e-filtro-relevancia.tex` (+ subseção "Direções para elevar a acurácia": alvo volatilidade, zona morta, modelo 2 estágios, aspect-based, saliência, Albertina, intradiário) via `\input` no Cap.4.

**VALIDAÇÃO DO SENTIMENTO FEITA (08/07/2026):** usuário rotulou os **300** do conjunto-ouro (Sentimento humano: Neutro124/Positivo96/Negativo80; **Relevante_PETR4: só 37% Sim** — 63% não relevantes, quantifica o ruído; Direção esperada: Indefinida240/Alta39/Baixa21). `avaliar_conjunto_ouro_petr4.py` → **FinBERT vs humano: acurácia 58,0%** (reponderada 57,65%), **Kappa de Cohen 0,371** (razoável/fair, quase moderada), F1-macro 57,9%. Matriz: modelo **superestima Negativo** (revocação Neg 0,75, precisão 0,53). Relatório em `Mestrado_PETR4/conjunto_ouro/relatorio_validacao_ouro.txt`. Dissertação: `capitulos/4g-validacao-sentimento.tex` (\input Cap.4). **Interpretação-chave:** o sentimento é só "razoável" → MELHORÁVEL (Albertina/fine-tuning ou mais rótulos) — diferente do teto da direção (mercado). O 37% de relevância humana motiva um classificador de relevância aprendido (trabalho futuro). Planilha rotulada aprimorada com dropdowns está em conjunto_ouro_para_rotular.xlsx (backup _ORIGINAL).

**AMPLIAÇÃO de rótulos (Etapa A, 10/07/2026):** `src/sentimento/preparar_ampliacao_rotulagem.py` gerou `Mestrado_PETR4/conjunto_ouro/rotulagem_ampliacao.xlsx` (**400 manchetes NOVAS**, fora das 300) por **amostragem por incerteza (active learning)**: menor confiança do FinBERT (média 0,32 vs 0,70 do corpus), cotas Neutral150/Negative130/Positive120 (reforça Positivo), embaralhadas, menus suspensos, cega ao modelo. Gabarito do modelo p/ avaliação: `gabarito_ampliacao.csv` (IDs AMP0001+). Objetivo: chegar a ~700 rótulos e re-rodar o notebook Colab p/ ver se **BERTimbau-large** (que ficou a −1,7pp) passa o FinBERT. Após rotular: combinar 300+400, atualizar `conjunto_ouro_gabarito_modelo`+xlsx e re-treinar. Pendente: usuário rotular.

**Estratégia adotada (jul/2026): melhorar por PARTES** — 1º sentimento (validar/medir + melhorar encoder), 2º direção, 3º volatilidade, depois integrar. Motivo: pipeline é cadeia; medir cada elo antes de juntar. Documentado (humanizado) em `capitulos/4f-estrategia-refinamento.tex` (\input no Cap.4, após 4e).

**Planilha de rotulagem PRONTA (05/07/2026):** `src/sentimento/preparar_planilha_rotulagem.py` aprimorou `Mestrado_PETR4/conjunto_ouro/conjunto_ouro_para_rotular.xlsx` (backup em `_ORIGINAL.xlsx`) com **4 menus suspensos** (openpyxl DataValidation): Sentimento_Humano(Positivo/Negativo/Neutro), Relevante_PETR4(Sim/Não), Direcao_Esperada_PETR4(Alta/Baixa/Indefinida), Confianca_Rotulador(Alta/Média/Baixa) — 300 manchetes estratificadas, aba "Instruções" com rubrica, cabeçalho fixo. Rotulagem HUMANA cega ao modelo. Depois de rotular, `src/sentimento/avaliar_conjunto_ouro_petr4.py` (já existe, lê aba "Rotular"/col Sentimento_Humano, mapeia Positivo→Positive) calcula acurácia+Kappa de Cohen+F1+matriz. **Acurácia do sentimento na nossa base: AINDA NÃO MEDIDA** (0 rótulos); só temos distribuição FinBERT (Neg 48,5%/Neu 37,5%/Pos 14%) e confiança média 0,686 (≠ acurácia). Encoder melhor tende a melhorar o SENTIMENTO/Kappa, NÃO a direção (evidência: filtro de relevância neutro; teto = eficiência do mercado).

**Fine-tuning de ENCODER — RODOU (08-09/07/2026), resultado NEGATIVO honesto:** GPU do usuário é **AMD RX 6600 XT** (não NVIDIA) → sem CUDA no Windows (ROCm só Linux; DirectML quebra) → rodei em **CPU** (300 exemplos é pequeno, CPU basta). `12_finetune_albertina_ptbr.py` reescrito p/ **k-fold CV** (laço manual torch, sem datasets/accelerate), usa títulos do xlsx + rótulos FinBERT do gabarito como baseline. Resultado FINAL (Colab GPU T4, 5-fold, `resultado_encoders_petr4.csv`): **FinBERT 58,0%/Kappa0,370** (baseline) > **BERTimbau-large 56,33%/0,330 (−1,7pp)** > BERTimbau-base 52,67%/0,261 > **Albertina-100M 42,0%/0,033 (−16pp, colapsa)**. (No piloto CPU o Albertina deu 45,67%.) **Nenhum supera o FinBERT com 300 rótulos.** Sinal importante: **BERTimbau-large ficou a só −1,7pp** — mesma família do FinBERT, maior → com **mais rótulos (500-1000)** deve empatar/superar. Albertina(DeBERTa) exigiria muito mais rótulos ou pré-treino de domínio. **FinBERT segue a melhor escolha; via p/ superá-lo: rotular mais + BERTimbau-large.** Notebook Colab exigiu 2 correções: DeBERTa não aceita **fp16** (rodar fp32) e o checkpoint Albertina é **bf16** (carregar com `torch_dtype=float32).float()`). Dissertação: `capitulos/4h-experimento-encoder.tex` (\input Cap.4 após 4g). Resultados em `Mestrado_PETR4/experimentos_encoder/`. **Notebook Colab autônomo** (GPU T4 grátis, 300 exemplos EMBUTIDOS em base64, compara FinBERT×Albertina×BERTimbau base/large): `notebooks/finetune_encoder_colab.ipynb` (gerado por `docs/_gerar_notebook_colab.py`) — usuário só sobe e "Run all".

**Pasta renomeada** de `datasets/` → **`datasets_refino/`** (o nome `datasets` sombreava a lib HF `datasets`). **Relatório Word** do dia: `docs/Relatorio_Refino_Banca_Jul2026.docx` (gerado por `docs/_relatorio_refino.py`). PyMuPDF (`fitz`) disponível no env `petr4` p/ ler PDFs (poppler NÃO instalado → Read de PDF falha).

**Inconsistência a resolver:** Cap.2 diz **25 estudos**; deck do seminário diz **29**. Uniformizar (eu havia mudado o deck p/ 29 — provavelmente o correto é 25 do referencial). Ver [[seminario-julho2026]] e [[projeto-petr4-dissertacao]]. Encoders SOTA em [[encoders-sentimento-ptbr]].
